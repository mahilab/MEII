get_filename_component(MEII_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

find_package(MEL)

list(APPEND CMAKE_MODULE_PATH ${MEII_CMAKE_DIR})

list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET MEII::MEII)
    include("${MEII_CMAKE_DIR}/MEIITargets.cmake")
endif()

set(MEII_LIBRARIES MEII::MEII)